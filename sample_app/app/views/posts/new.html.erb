<h1>新規投稿</h1>
<!-- form forの部分 -->

<!-- %で始まるタグは、rubyで使われるタグです。特徴としてはタグの中身の結
果が画面に出力されず処理され、はタグの中身の結果が画面に出力されます。 -->

<%= form_for(@post)do|f|%>

	<h4>タイトル</h4>
	<%= f.text_field :title %>

	<h4>本文</h4>
	<%= f.text_area :body %>

	<h4>画像</h4>
	<%= f.attachment_field :image%>

	<%= f.submit '投稿' %>
<% end %>